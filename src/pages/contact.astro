---
import { Image } from 'astro:assets';
import myLogo from '../assets/myLogo.jpg';
---

<html lang="en">
	<head>
		<meta charset="utf-8" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="viewport" content="width=device-width" />
		<title>Contact | Karan Sekar AI</title>
	</head>
	<body>
		<header>
            <a href="/"><Image src={myLogo} alt="Logo" width={60} /></a>
        </header>

		<main>
			<h1>Initiate <span class="text-gradient">Protocol</span></h1>
			<p>Secure a consultation for your AI transformation.</p>

			<form action="https://formspree.io/f/YOUR_ID_HERE" method="POST">
				<input type="text" name="name" placeholder="Name/Agency" required />
				<input type="email" name="email" placeholder="hello@karansekar.com" required />
				<textarea name="message" placeholder="To architect the digital backbone of modern industries by bridging the gap between legacy tradition and the limitless potential of Edge-computing and AI-driven automation." rows="5" required></textarea>
				<button type="submit" class="cta-button">Send Transmission</button>
			</form>
		</main>
	</body>
</html>

<style>
	:global(body) {
		background-color: #0B0D17;
		color: #F0F4F8;
		font-family: 'Inter', sans-serif;
		display: flex;
		flex-direction: column;
		align-items: center;
		margin: 0;
		padding: 2rem;
	}

	header { width: 100%; max-width: 800px; margin-bottom: 4rem; }

	main { text-align: center; max-width: 600px; width: 100%; }

	h1 { font-size: 3rem; }

	.text-gradient {
		background: linear-gradient(45deg, #00A896, #FDB813);
		-webkit-background-clip: text;
		-webkit-text-fill-color: transparent;
	}

	form {
		display: flex;
		flex-direction: column;
		gap: 1.5rem;
		margin-top: 3rem;
		background: rgba(255, 255, 255, 0.03);
		padding: 2rem;
		border-radius: 20px;
		border: 1px solid rgba(0, 168, 150, 0.2);
	}

	input, textarea {
		background: rgba(255, 255, 255, 0.05);
		border: 1px solid rgba(255, 255, 255, 0.1);
		padding: 1rem;
		color: white;
		border-radius: 8px;
		font-family: inherit;
	}

	input:focus, textarea:focus {
		outline: none;
		border-color: #00A896;
		background: rgba(0, 168, 150, 0.05);
	}

	.cta-button {
		background: #00A896;
		color: white;
		border: none;
		padding: 1rem;
		border-radius: 8px;
		font-weight: bold;
		cursor: pointer;
		transition: 0.3s;
	}

	.cta-button:hover {
		background: #FDB813;
		transform: scale(1.02);
	}
</style>