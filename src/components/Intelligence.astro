<section id="intelligence" class="intelligence">
  <div class="section-header">
    <h2 class="text-gradient">Platform Intelligence</h2>
  </div>

  <div class="intel-grid">
    <div class="intel-card">
      <div class="icon">🧠</div>
      <h3>AI Automation</h3>
      <p>
        Integrated workflows that capture and qualify leads while you sleep.
        We automate the repetitive, so you can focus on the strategic.
      </p>
    </div>

    <div class="intel-card">
      <div class="icon">🛡️</div>
      <h3>Privacy by Design</h3>
      <p>
        Native CASL and PIPEDA compliance baked into every form,
        protecting your professional reputation and your clients' data.
      </p>
    </div>

    <div class="intel-card">
      <div class="icon">⚡</div>
      <h3>Edge Performance</h3>
      <p>
        Built on Astro for lightning-fast load times.
        We ensure your platform outperforms the competition on every device.
      </p>
    </div>
  </div>
</section>

<style>
  .intelligence {
    padding: 8rem 1.5rem;
    text-align: center;
    max-width: 1100px;
    margin: 0 auto;
  }

  .section-header h2 {
    font-size: 3rem;
    font-weight: 800;
    margin: 0 0 5rem 0;
    color: #fff;
  }

  .text-gradient {
    background: linear-gradient(to right, #34d399, #a3e635);
    -webkit-background-clip: text;
    background-clip: text;
    -webkit-text-fill-color: transparent;
    color: #34d399;
  }

  .intel-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 2.5rem;
  }

  .intel-card {
    padding: 3.5rem 2.5rem;
    background: rgba(255, 255, 255, 0.02);
    border: 1px solid rgba(255, 255, 255, 0.08);
    border-radius: 20px;
    text-align: left;
    transition: 0.35s cubic-bezier(0.4, 0, 0.2, 1);
  }

  .intel-card:hover {
    border-color: rgba(251, 191, 36, 0.6);
    transform: translateY(-8px);
    background: rgba(255, 255, 255, 0.05);
  }

  .icon {
    font-size: 2.5rem;
    margin-bottom: 1.5rem;
  }

  .intel-card h3 {
    font-size: 1.6rem;
    color: #ffffff;
    margin: 0 0 1.2rem 0;
  }

  .intel-card p {
    line-height: 1.8;
    color: #94a3b8;
    font-size: 1.05rem;
    margin: 0;
  }
</style>
