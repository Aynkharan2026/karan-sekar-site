---
/* Hero Component for KaranSekar.com */
---

<section class="hero">
    <div class="badge-container">
        <span class="agency-badge">AI-DRIVEN INFRASTRUCTURE</span>
    </div>

    <h1>
        Architecting High-Performance Platforms<br />
        For a <span class="text-gradient">Global Market</span> — We Handle the Complexity
    </h1>

    <p class="tagline">Engineered for the Modern Professional</p>

    <p class="description">
        Specializing in fast, secure, and automated web systems with native support for the languages that drive your business.
    </p>

    <div class="cta-group">
        <a href="/contact" class="primary-btn">Start Your Project</a>
        <a href="#advantage" class="secondary-btn">Explore the Global Edge →</a>
    </div>
</section>

<style>
    .hero { 
        text-align: center; 
        padding: 8rem 1.5rem 4rem; 
        max-width: 1100px; 
        margin: 0 auto; 
    }

    .agency-badge { 
        background: rgba(52, 211, 153, 0.1); 
        border: 1px solid #10b981; 
        color: #34d399; 
        padding: 0.5rem 1.2rem; 
        border-radius: 99px; 
        font-size: 0.8rem; 
        font-weight: 700; 
    }

    .hero h1 { 
        font-size: clamp(2rem, 5vw, 3.8rem); 
        line-height: 1.2; 
        margin: 2rem 0; 
        color: #ffffff;
    }

    .text-gradient { 
        background: linear-gradient(to right, #34d399, #a3e635); 
        -webkit-background-clip: text; 
        background-clip: text; 
        -webkit-text-fill-color: transparent; 
        color: #34d399; 
    }

    .gold-text { 
        color: #fbbf24; 
    }

    .tagline { 
        color: #94a3b8; 
        text-transform: uppercase; 
        letter-spacing: 3px; 
        font-size: 0.9rem; 
    }

    .description { 
        font-size: 1.2rem; 
        color: #cbd5e1; 
        max-width: 750px; 
        margin: 0 auto 3rem; 
        line-height: 1.8; 
    }

    .cta-group { 
        display: flex; 
        gap: 1.5rem; 
        justify-content: center; 
        flex-wrap: wrap; 
    }

    .primary-btn { 
        background: #10b981; 
        color: #000; 
        padding: 1.2rem 2.5rem; 
        border-radius: 12px; 
        font-weight: 800; 
        text-decoration: none; 
        display: inline-block; 
        transition: 0.3s; 
    }

    .primary-btn:hover { 
        background: #34d399; 
        transform: translateY(-3px); 
    }

    .secondary-btn { 
        border: 1px solid rgba(255, 255, 255, 0.3); 
        color: #fff; 
        padding: 1.2rem 2.5rem; 
        border-radius: 12px; 
        text-decoration: none; 
        font-weight: 600; 
        transition: 0.3s; 
    }
    
    .secondary-btn:hover {
        border-color: #fbbf24;
        color: #fbbf24;
    }
</style>