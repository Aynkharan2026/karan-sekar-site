---
const BASE = import.meta.env.BASE_URL;
---

<section class="hero">
  <div class="hero-content">
    <div class="badge-container">
      <span class="agency-badge">AI-DRIVEN GLOBAL ARCHITECTURE</span>
    </div>

    <h1>
      Architecting High-Performance Platforms For a
      <span class="text-gradient">Global Market</span>
    </h1>

    <p class="description">
      Building automated, multilingual systems for English, Tamil, Mandarin, and Spanish markets.
    </p>

    <div class="contact-strip">
      <a href="mailto:hello@karansekar.com" class="contact-link">
        <span>✉️</span> hello@karansekar.com
      </a>
      <a href="https://wa.me/14163885434" target="_blank" rel="noopener" class="contact-link">
        <span>💬</span> +1 (416) 388 5434 (WhatsApp)
      </a>
    </div>
  </div>

  <div class="hero-form-container">
    <form action="https://formspree.io/f/xnjbnpdk" method="POST" class="quick-form">
      <input type="hidden" name="_next" value={`${BASE}success`} />
      <h3>Initiate Protocol</h3>
      <p>Connect with Karan for a technical consultation.</p>

      <div class="form-inputs">
        <label>Name</label>
        <input type="text" name="name" placeholder="Full Name" required />

        <label>Email</label>
        <input type="email" name="email" placeholder="you@example.com" required />

        <label>Transmission</label>
        <textarea name="message" placeholder="Project details..." rows="3"></textarea>
      </div>

      <button type="submit" class="primary-btn">Send Transmission</button>
    </form>
  </div>
</section>

<style>
  .hero {
    display: grid;
    grid-template-columns: 1.2fr 1fr;
    gap: 4rem;
    padding: 6rem 5%;
    max-width: 1250px;
    margin: 0 auto;
    align-items: center;
  }

  @media (max-width: 1024px) {
    .hero {
      grid-template-columns: 1fr;
      text-align: center;
    }
  }

  .badge-container {
    margin-bottom: 1.5rem;
  }

  .agency-badge {
    display: inline-block;
    padding: 0.6rem 1rem;
    border-radius: 999px;
    font-weight: 800;
    letter-spacing: 0.08em;
    font-size: 0.75rem;
    background: rgba(16, 185, 129, 0.12);
    border: 1px solid rgba(16, 185, 129, 0.35);
    color: #a7f3d0;
  }

  h1 {
    font-size: 3.2rem;
    line-height: 1.05;
    margin: 0;
    font-weight: 800;
    color: #fff;
  }

  @media (max-width: 1024px) {
    h1 {
      font-size: 2.5rem;
    }
  }

  .description {
    margin-top: 1.5rem;
    font-size: 1.1rem;
    line-height: 1.7;
    color: #94a3b8;
    max-width: 46ch;
  }

  @media (max-width: 1024px) {
    .description {
      margin-left: auto;
      margin-right: auto;
    }
  }

  .text-gradient {
    background: linear-gradient(to right, #34d399, #a3e635);
    -webkit-background-clip: text;
    background-clip: text;
    -webkit-text-fill-color: transparent;
  }

  .contact-strip {
    display: flex;
    flex-direction: column;
    gap: 1rem;
    margin-top: 2rem;
    text-align: left;
  }

  @media (max-width: 1024px) {
    .contact-strip {
      align-items: center;
      text-align: center;
    }
  }

  .contact-link {
    color: #fff;
    text-decoration: none;
    font-weight: 600;
    display: inline-flex;
    align-items: center;
    gap: 10px;
    transition: 0.25s;
  }

  .contact-link:hover {
    color: #fbbf24;
  }

  .hero-form-container {
    background: #0d1224;
    padding: 3rem;
    border-radius: 30px;
    border: 1px solid rgba(16, 185, 129, 0.3);
  }

  .quick-form h3 {
    margin: 0;
    font-size: 1.6rem;
    font-weight: 800;
  }

  .quick-form p {
    margin-top: 0.75rem;
    margin-bottom: 0;
    color: #94a3b8;
    line-height: 1.6;
  }

  .form-inputs {
    display: flex;
    flex-direction: column;
    gap: 1rem;
    text-align: left;
    margin: 1.5rem 0;
  }

  label {
    font-size: 0.8rem;
    color: #94a3b8;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.06em;
  }

  input,
  textarea {
    background: #070b14;
    border: 1px solid rgba(255, 255, 255, 0.1);
    padding: 1rem;
    color: #fff;
    border-radius: 12px;
    width: 100%;
    box-sizing: border-box;
  }

  input:focus,
  textarea:focus {
    outline: none;
    border-color: rgba(52, 211, 153, 0.9);
  }

  .primary-btn {
    width: 100%;
    background: #10b981;
    color: #000;
    padding: 1.2rem;
    border-radius: 12px;
    font-weight: 800;
    border: none;
    cursor: pointer;
    transition: 0.25s;
  }

  .primary-btn:hover {
    background: #34d399;
    transform: translateY(-2px);
  }
</style>
